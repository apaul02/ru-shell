[package]
name = "rusty_shell"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.2", features = ["signal", "term", "process", "fs"] }
rustyline = { version = "17.0.2", features = ["derive"] }
